<%= form_for(@page_view) do |f| %>
  <% if @page_view.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_view.errors.count, "error") %> prohibited this page_view from being saved:</h2>

      <ul>
      <% @page_view.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_area :url %>
  </div>
  <div class="field">
    <%= f.label :time %><br />
    <%= f.datetime_select :time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
